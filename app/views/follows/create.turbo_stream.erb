<%# turbo_stream.update "follow_#{@user.id}", partial: "link", locals: { user_id: @user.id, follow_id: @follow.id } %>
<%= turbo_stream.update "user_link_#{@user.id}", partial: "follow_link", locals: { user: @user } %>
